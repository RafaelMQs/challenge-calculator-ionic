import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'challenge-calculator-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
